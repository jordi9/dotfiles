# AGENTS.md — ~/dev

`~/dev` contains independent repositories, not a monorepo.

- Work from the target repository and follow its local `AGENTS.md`.
- Do not assume repositories share dependencies, commands, or history.
- Use `jj` in jj-managed repositories.
- Do not run broad destructive commands from `~/dev`.
- Do not add backward-compatibility behavior unless requested; ask rather than guess.
